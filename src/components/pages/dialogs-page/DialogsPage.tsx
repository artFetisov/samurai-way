import {FC} from "react";
import {useAuthRedirect} from "../../../hooks/useAuthRedirect";

export const DialogsPage: FC = () => {
    // useAuthRedirect()

    return <div>
        dialogs page
    </div>
}